import fetch from 'node-fetch';

async function testApi() {
  try {
    // Usaremos um token válido ou forçaremos se soubermos, mas não temos token de auth fácil.
    // Vamos chamar o QueryOptimizer instanciando.
  } catch(e) {
    console.error(e);
  }
}
testApi();
